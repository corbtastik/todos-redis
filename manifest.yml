---
applications:
- name: todos-redis
  memory: 1G
  path: target/todos-redis-1.0.0.SNAP.jar
  buildpack: java_buildpack_offline
  services:
  - todos-redis
  - todos-config
  - todos-registry
  env:
    TRUST_CERTS: api.sys.retro.io
    TODOS_API_LIMIT: 4096